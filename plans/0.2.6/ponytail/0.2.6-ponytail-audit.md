# 0.2.6 Ponytail audit - current-tree cleanup ledger

> **Parent:** `plans/0.2.6/README.md` - post-ship review work for the current 0.2.6 tree
> **Status:** audit complete. All 40 remediation rows are intentionally unstarted.
> **Estimated effort:** 6-12 focused days, split into small independently validated changes.
> **Date:** 2026-09-12
> **Scope:** 185 production Go files and 292 Go test files. This is a deletion and simplification audit only. It does not file correctness, security, or performance defects.

---

## Overview

Four read-only reviewers scanned separate packages: public boundaries, CSS and conversion, layout, and PDF/image output. They checked current callers before proposing a deletion. The prior completed Ponytail report at `plans/0.2.6/review/ponytail-go-audit-2026-08-30.md` was a false-positive filter. No completed `PT-GO-*` row is reopened here.

The 40 rows below are the canonical implementation ledger. The packet reports provide evidence but carry no checklist state. This audit made no production change, so every remediation row stays `[ ]` until its own code and validation land.

## Executive summary

The highest-value cuts are inactive layout experiments: the unused parallel layout API, section-chrome cloning code, and metadata handoff used only by tests. The next group removes CSS properties that are stored, inherited, and interned but never read by layout or paint. The lower-risk work deletes uncalled wrappers, test-only facades, and hand-written standard-library clones.

Six output-path ideas were not made active because they intentionally trade CPU time, cached memory, or encoded bytes. They are recorded in `04-output.md` as qualified leads, not implementation work.

## Phase 1: public boundary dead code and thin wrappers

### 1.1 Loader and CLI

- [ ] **PT26-BND-01 [delete]** Delete the uncalled deprecated `NewLoader` compatibility constructor at `internal/load/load.go:494-522`; use `NewLoaderWithError` as the only constructor. Proof: `go test ./internal/load`, then the Phase 8 gates.
- [ ] **PT26-BND-02 [delete]** Remove doc-flag table registration, `addDocFlags`, and `nopFlag` at `internal/cli/flags.go:71,87-94,476`; `parseState.step` handles the four flags before lookup at `internal/cli/cli.go:185-188`. Proof: `go test ./internal/cli`, then the Phase 8 gates.
- [ ] **PT26-BND-03 [delete]** Delete uncalled `ParseMode` at `internal/cli/cli.go:270-274`; callers already use `Parse`. Proof: `go test ./internal/cli`, then the Phase 8 gates.
- [ ] **PT26-BND-04 [shrink]** Replace the one-consumer `boolFlagState` carrier at `internal/cli/cli.go:427-431,478-499` with the three values passed directly to `parseBoolFlag`. Proof: `go test ./internal/cli`, then the Phase 8 gates.
- [ ] **PT26-BND-05 [shrink]** Inline one-caller `isShortFlag` at `internal/cli/cli.go:197,230-233` with its `strings.HasPrefix` and length check. Proof: `go test ./internal/cli`, then the Phase 8 gates.

### 1.2 Settings, public API, and HTML

- [ ] **PT26-BND-06 [yagni]** Replace the `ColorMode` type and formatter/parser at `internal/settings/settings.go:92-125` and `internal/settings/reflect.go:394-405` with the two accepted strings in the sole grayscale setter. Preserve the same accepted input and error. Proof: `go test ./internal/settings`, then the Phase 8 gates.
- [ ] **PT26-BND-07 [shrink]** Delete the one-caller duplicate margin switch `marginValue` at `internal/settings/reflect.go:425-440,698-703`; dereference `marginEdgePtr` in the getter. Proof: `go test ./internal/settings`, then the Phase 8 gates.
- [ ] **PT26-BND-08 [shrink]** Merge `ApplyImageKeyNormalized` at `internal/settings/reflect.go:1046-1064` into its only caller `ApplyImageKey`. Proof: `go test ./internal/settings`, then the Phase 8 gates.
- [ ] **PT26-BND-09 [shrink]** Inline test-only `StampEmptyHFOverride` at `internal/settings/object_roles.go:3-25` into `StampCover`. Proof: `go test ./internal/settings`, then the Phase 8 gates.
- [ ] **PT26-BND-10 [stdlib]** Replace `documentCloneStrings` and `documentCloneStringMap` at `document.go:415,425-426,496-497,580,632,637-643` with `slices.Clone` and `maps.Clone`. Proof: `go test .`, then the Phase 8 gates.
- [ ] **PT26-BND-11 [stdlib]** Replace the local `cloneBytes` at `api.go:198-207` with `slices.Clone` at its two document callers. Proof: `go test .`, then the Phase 8 gates.
- [ ] **PT26-BND-12 [delete]** Delete uncalled `isTrimSpace` at `internal/html/html.go:587-596`; the current parser uses `strings.TrimSpace`. Proof: `go test ./internal/html`, then the Phase 8 gates.

## Phase 2: CSS parser and matcher reductions

- [ ] **PT26-CSS-01 [shrink]** Replace the process-global sibling LRU at `internal/css/match.go:14-183` with local sibling scans in `getSiblingInfo`; remove cache-only tests. This is a complexity cut with a benchmark check before closing. Proof: targeted sibling, nth, and `:has()` tests plus an existing CSS benchmark comparison.
- [ ] **PT26-CSS-02 [shrink]** Make `containsWord` at `internal/css/match.go:481-513` delegate to equivalent `hasClassToken` at `internal/css/match.go:755-782`. Proof: `go test ./internal/css -run 'Test(AttrWordAndSubstring|Match)'`.
- [ ] **PT26-CSS-03 [delete]** Read `namedColorTable` directly from `ParseColor` and delete one-caller `namedColors` at `internal/css/values.go:219,851-854`. Proof: `go test ./internal/css -run TestParseColor`.
- [ ] **PT26-CSS-04 [stdlib]** Replace manual ASCII `hasFoldPrefix` at `internal/css/css.go:231-256` with a length guard plus `strings.EqualFold`. Proof: parser at-rule tests and the existing `FuzzParseCSS` corpus.
- [ ] **PT26-CSS-05 [stdlib]** Delete the uppercase pre-scan in `LengthToPt` at `internal/css/container.go:116-125`; `strings.ToLower` already accepts lower-case input. Proof: `go test ./internal/css -run 'TestLengthToPt|Test(MediaMatchesSizeFeatures|ContainerCondMatches)'`.

## Phase 3: conversion adapters and benchmark-only code

- [ ] **PT26-CNV-01 [shrink]** Remove unused `log io.Writer` from `renderIndependentBlocks` at `internal/convert/page_blocks.go:13-60` and its call at `internal/convert/convert.go:642`. Proof: `go test ./internal/convert -run 'Test(PageAtATimeFiveSections|SectionTextCountMatchesOpText|IndependentBlocksFallbackSpanningTable)'`.
- [ ] **PT26-CNV-02 [delete]** Delete compatibility-only `drawHeadersFooters` and `hfDrawResult.emitWarnings` at `internal/convert/hf.go:500-506,745-754`; production uses `drawHeadersFootersResult`. Proof: targeted header/footer tests.
- [ ] **PT26-CNV-03 [delete]** Remove the test-only benchmark page-island path at `internal/convert/convert.go:65-69,123-135,630-638`, `internal/convert/page_islands.go`, and `internal/convert/islands/plan.go`; keep the generic renderer. Proof: compare generic output before removal, then `go test ./internal/convert` and `make golden`.
- [ ] **PT26-CNV-04 [delete]** Delete test-only `SimplifyDOMEnabled` and `SimplifyDOMProfile` at `internal/convert/prepare/simplify.go:27-45`; test `BuildOptions` directly. Proof: targeted prepare and simplify tests.
- [ ] **PT26-CNV-05 [delete]** Delete test-only `CollectSheets` and `MergeFontFaces` wrappers at `internal/convert/prepare/styles.go:38-43,439-444`; tests construct `ResourceContext` directly. Proof: targeted prepare, convert, and image font-face tests.
- [ ] **PT26-CNV-06 [delete]** Delete test-only exported `LinkStylesheet` at `internal/convert/prepare/styles.go:433-437`; move its white-box test into `prepare`. Proof: targeted prepare tests and `TestLinkStylesheetMediaMatches`.
- [ ] **PT26-CNV-07 [delete]** Inline one-caller `ValidateRenderableObjects` at `internal/convert/convert.go:162,270-278` to `settings.ValidateRenderableObjects`. Proof: targeted request validation tests.

## Phase 4: layout experiments and unused style state

- [ ] **PT26-LAY-01 [delete]** Delete test-only parallel layout experiment `internal/layout/parallel.go:17-454` and its tests. There is no production `ParallelLayout` caller. Proof: run parallel tests before removal, then `go test ./internal/layout` and `make golden`.
- [ ] **PT26-LAY-02 [delete]** Delete unused section-cloning experiment at `internal/layout/section_clone.go:14-138` and its test-only callers. Proof: targeted section clone and independent-block tests before removal, then layout tests and `make golden`.
- [ ] **PT26-LAY-03 [delete]** Delete test-only `PaintMetadata`, `CopyPaintMetadata`, and `locationsFromBoxes` at `internal/layout/independent_blocks.go:11-86`. Proof: `go test ./internal/layout -run IndependentBlocks`.
- [ ] **PT26-LAY-04 [yagni]** Delete unread image, color-scheme, containment, and content-visibility fields from `internal/layout/style.go:348-362` with parse, inheritance, generated-interning, mapping, and test updates. Keep `MarginTrim`, which layout reads. Proof: targeted layout tests and `make golden`.
- [ ] **PT26-LAY-05 [yagni]** Delete unread variable-font, bidi, and decoration state in `internal/layout/style.go:363-375` with its parse, inheritance, generated-interning, mapping, and test updates. Keep `TextDecorationSkipInk`, which paint reads. Proof: targeted layout tests and `make golden`.
- [ ] **PT26-LAY-06 [delete]** Delete unreachable `emitInlineBullet` / `hasInlineBullet` at `internal/layout/inline_paint.go:1236-1294` and its private marker helper chain in `pseudo_content.go:500-553`. Proof: list tests and `make golden`.
- [ ] **PT26-LAY-07 [delete]** Delete unused logical-border utility chain at `internal/layout/style_logical_border.go:9-102`, while retaining logical-radius support below line 120. Proof: logical-border tests and `make golden`.

## Phase 5: layout views and caches that need a measurement guard

- [ ] **PT26-LAY-08 [yagni]** Remove the per-engine glyph cache at `internal/layout/advance_cache.go:13-44` and use `face.AdvanceInPoints` at its three callers. Proof: layout and golden tests plus a before/after benchmark record.
- [ ] **PT26-LAY-09 [yagni]** Remove style-resolution memoization at `internal/layout/style_memo.go:10-153` and its dispatch in `style.go:508,625`. Proof: layout and golden tests plus a before/after benchmark record.
- [ ] **PT26-LAY-10 [shrink]** Replace private `boxModelStyle` / `boxModelStyleOf` at `internal/layout/style_views.go:3-69` with `*ResolvedStyle` in same-package sizing helpers. Proof: layout tests and `make golden`.
- [ ] **PT26-LAY-11 [shrink]** Replace `paintChromeStyle` / `paintChromeStyleOf` at `internal/layout/paint_style_view.go:3-33` with existing `boxNode.style` after current nil checks. Proof: chrome, outline, and overflow tests, then `make golden`.
- [ ] **PT26-LAY-12 [delete]** Delete unread `ResolvedStyle.VertChrome` at `internal/layout/layout.go:1261-1268`; retain used `HorizChrome`. Proof: `go test ./internal/layout`.

## Phase 6: PDF and image output cleanup

- [ ] **PT26-OUT-01 [delete]** Remove the process-global semantic regex LRU at `internal/pdf/semantic.go:15-105`; compile through the existing error path and delete cache-only assertions. Proof: `go test ./internal/pdf -run TestSemantic`.
- [ ] **PT26-OUT-02 [delete]** Delete custom YCbCr 4:2:0 preconversion at `internal/imageout/ycbcr.go:1-85` and encode the existing NRGBA with `jpeg.Encode`. Existing tests assert byte parity. Proof: targeted JPEG encoding tests.
- [ ] **PT26-OUT-03 [delete]** Delete `_test.go` CLI adapter facade at `internal/imageout/compat_test.go:1-53` and migrate its five tests to `Request` / `RunRequest` or `app.RunImage`. Proof: `go test ./internal/imageout`.
- [ ] **PT26-OUT-04 [delete]** Delete assertion-free manual debug output at `internal/imageout/debug_test.go:1-41`; retain data-URI and image-gate assertions. Proof: `go test ./internal/imageout`.

## Phase 7: closure gates

- [ ] **PT26-GATE-01** After each landed row, run its named targeted command and record the exit result beside that row before changing it to `[x]`.
- [ ] **PT26-GATE-02** After each layout or conversion row, run `make golden` and record the 65-fixture result before closing that row.
- [ ] **PT26-GATE-03** Before closing any cache or fast-path row, record a before/after benchmark using the existing affected-package benchmark. Do not call a passing test a performance result.
- [ ] **PT26-GATE-04** At the end of an implementation wave, run `make test`, `make claim-scan`, and `make lint` on the final tree. Rows remain open if any gate fails.
- [ ] **PT26-GATE-05** Update `knowledge-base/wiki/` only for rows that actually land, then scan this ledger for `[~]` and `[ ]` before claiming the implementation wave is complete.

## Dependencies

```text
Phase 1: independent low-risk boundary cuts
Phase 2: independent CSS cuts
Phase 3: conversion wrappers before benchmark-island deletion
Phase 4: delete dormant layout paths before changing style storage
Phase 5: measure cache/view removals after Phase 4 settles ResolvedStyle
Phase 6: independent output and test cleanup
Phase 7: after every implemented row, then once on the final tree
```

## Evidence boundary

This is a current-source audit. The reviewers did not run a benchmark or modify production code. Estimated line counts are intentionally kept in packet reports because shared imports and migrated tests change the final number. Each ledger row names the proof required to close it.

net: about -3,500 source and test lines possible, -0 dependencies possible.
