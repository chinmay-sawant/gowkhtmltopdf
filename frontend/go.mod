// Separate module so frontend/ is not packed into the parent module zip
// (go install of the CLIs does not need the documentation site).
module github.com/chinmay-sawant/gowkhtmltopdf/frontend

go 1.26
