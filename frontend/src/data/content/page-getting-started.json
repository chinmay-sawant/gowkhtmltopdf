{
 "id": "getting-started",
 "nav": "Getting Started",
 "content": [
  {
   "type": "hero",
   "title": "Your first PDF in five minutes",
   "lede": "Install the two CLIs with go install, or build them from a checkout. No cgo or browser process. Then turn a local HTML file into a PDF with a single opt-in flag."
  },
  {
   "type": "bullets",
   "heading": "Requirements",
   "items": [
    "Go 1.26 or newer. The first build may download the two allowlisted direct modules and their transitive Go module graph; later builds can run offline with the cache populated.",
    "No cgo and no native libraries. Build with CGO_ENABLED=0 to get statically linked ELF/Mach-O binaries with no runtime library requirements.",
    "No browser, WebKit, or third-party HTML-to-PDF service. The whole pipeline (load, parse, style, layout, paint, PDF write) is implemented in this repository.",
    "The two allowlisted direct pure-Go modules are github.com/go-text/typesetting for OpenType shaping and github.com/tdewolff/canvas for SVG rasterization; there is no browser or native converter dependency."
   ]
  },
  {
   "type": "code",
   "heading": "Install with Go",
   "language": "sh",
   "code": "go install github.com/chinmay-sawant/gowkhtmltopdf/cmd/gowkhtmltopdf@v0.2.4\ngo install github.com/chinmay-sawant/gowkhtmltopdf/cmd/gowkhtmltoimage@v0.2.4\ngowkhtmltopdf --version   # Name: gowkhtmltopdf / Version: 0.2.4"
  },
  {
   "type": "code",
   "heading": "Build",
   "language": "sh",
   "code": "# recommended: static binaries, version stamped from VERSION\nmake build\n# writes bin/gowkhtmltopdf and bin/gowkhtmltoimage\n\n# equivalent, explicit:\nCGO_ENABLED=0 go build -ldflags \"-X github.com/chinmay-sawant/gowkhtmltopdf/internal/cli.Version=$(cat VERSION)\" \\\n  -o bin/gowkhtmltopdf ./cmd/gowkhtmltopdf\nCGO_ENABLED=0 go build -ldflags \"-X github.com/chinmay-sawant/gowkhtmltopdf/internal/cli.Version=$(cat VERSION)\" \\\n  -o bin/gowkhtmltoimage ./cmd/gowkhtmltoimage\n\n./bin/gowkhtmltopdf --version   # Name: gowkhtmltopdf / Version: 0.2.4\n# VERSION is the project release. LibraryVersion (0.12.7-dev) is the\n# wkhtmltopdf settings-surface id, not the release number."
  },
  {
   "type": "callout",
   "variant": "tip",
   "title": "Prebuilt binaries",
   "body": "Tagged releases publish static gowkhtmltopdf and gowkhtmltoimage builds for Linux, Windows, and macOS (amd64 and arm64) plus SHA256SUMS. Download from the GitHub Releases page. Ordinary CI on pull requests does not upload platform archives."
  },
  {
   "type": "code",
   "heading": "First local PDF",
   "language": "sh",
   "code": "./bin/gowkhtmltopdf --allow-local-files -o /tmp/invoice.pdf \\\n  testdata/golden/fixture-01-simple-invoice.html"
  },
  {
   "type": "code",
   "heading": "Opt-in PDF version or profile (0.2.2)",
   "language": "sh",
   "code": "# Default is unclaimed PDF 1.4. --pdf-version is not a PDF/A or PDF/UA claim.\n./bin/gowkhtmltopdf --pdf-version 1.7 --allow-local-files -o /tmp/invoice-1.7.pdf \\\n  testdata/golden/fixture-01-simple-invoice.html\n\n# Dual PDF/A-3a + PDF/UA-1 (implies PDF 1.7)\n./bin/gowkhtmltopdf --pdf-profile a3a-ua1 --allow-local-files -o /tmp/invoice-a3a-ua1.pdf \\\n  testdata/golden/fixture-01-simple-invoice.html"
  },
  {
   "type": "code",
   "heading": "First image",
   "language": "sh",
   "code": "./bin/gowkhtmltoimage --allow-local-files --width 1024 -o /tmp/invoice.png \\\n  testdata/golden/fixture-01-simple-invoice.html"
  },
  {
   "type": "callout",
   "variant": "warn",
   "title": "Local files are blocked by default",
   "body": "Every local file read goes through a security ACL, so you must opt in with --allow-local-files on the CLI (or Document.AllowLocalFiles in the library API). Remote URLs are fetched with timeouts, redirect limits, and size caps, but never hand untrusted user URLs straight to the renderer: that is the classic SSRF pattern here and in upstream wkhtmltopdf."
  },
  {
   "type": "prose",
   "heading": "After the conversion",
   "sections": [
    {
     "heading": "Open the result",
     "body": "Open /tmp/invoice.pdf in any PDF viewer. Committed sample outputs live in output/ and are regenerated with make samples."
    },
    {
     "heading": "PDF bytes to stdout",
     "body": "Pass - as the output path to write the raw PDF bytes to stdout instead of a file. Pair it with --quiet so progress messages do not pollute the stream.",
     "bullets": [
      "gowkhtmltopdf --allow-local-files --quiet -o - report.html > report.pdf",
      "The clean byte stream pipes directly into an HTTP response or a downstream tool."
     ]
    },
    {
     "heading": "Repeatable output",
     "body": "The same HTML, settings, and metadata time produce a repeatable PDF structure. Default CLI conversions use the wall clock for PDF dates and [date]/[time] placeholders, so bytes are not hash-stable unless you inject Now on the typed library API."
    }
   ]
  },
  {
   "type": "table",
   "heading": "Makefile targets",
   "headers": [
    "Target",
    "What it runs"
   ],
   "rows": [
    [
     "test",
     "go test ./... - the full test suite"
    ],
    [
     "build",
     "Builds both static binaries (gowkhtmltopdf, gowkhtmltoimage) into bin/"
    ],
    [
     "lint",
     "golangci-lint run ./... with the pinned version (default v1.64.8), then frontend npm run lint (ESLint plus src/data content and config checks)"
    ],
    [
     "fmt",
     "gofmt -w ."
    ],
    [
     "golden",
     "go test ./internal/convert/ -run 'TestGoldenCorpus' -v - walks every testdata/golden/*.html fixture and asserts PDF structure, embedded font, page envelope, and feature expectations"
    ],
    [
     "golden-update GOLDEN_FIXTURE=... GOLDEN_APPROVE=1",
     "Generates one explicitly approved fixture PDF under ignored testdata/golden/out/; never rewrites fixture sources"
    ],
    [
     "samples",
     "Regenerates output/: one PDF per golden fixture, a showcase TOC/header/footer/outline PDF, image PNGs, and a soft-fail live Wikipedia smoke"
    ],
    [
     "bench",
     "In-process Go benchmark matrix (PDF, template, image tiles)"
    ],
    [
     "bench-cli-compare",
     "Process-level CLI comparison against installed wkhtmltopdf"
    ],
    [
     "claim-scan",
     "Fail on forbidden over-claim phrases in user-facing docs"
    ],
    [
     "clean",
     "rm -rf testdata/golden/out"
    ]
   ]
  }
 ]
}
